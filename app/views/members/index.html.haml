.members
  - @members.each do |member|
    = link_to "#{member.name}", member_path(member.id)

.container
  %h1 Chart.js
  %div
    %canvas#myChart

= link_to "カテゴリ別ページへ", categories2_index_path
= link_to "月別ページへ", year_month_path(Date.today.year, Date.today.month)